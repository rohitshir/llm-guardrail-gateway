services:
  llm-guardrail-gateway:
    build: .
    container_name: llm-guardrail-gateway
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./policies:/app/policies
    restart: unless-stopped
